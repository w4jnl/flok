# flok M0 spike: outer server configuration.
# Everything here is about staying invisible: no prefix, no status bar, pass every key and
# mouse event to the inner tmux client that lives in the right pane.
set -g prefix None
set -g prefix2 None
set -g status off
set -g mouse on
set -g set-clipboard on
set -g allow-passthrough on
set -g focus-events on
set -s extended-keys on
set -s extended-keys-format csi-u
set -s escape-time 0
set -g default-terminal "tmux-256color"
set -as terminal-features ',xterm-ghostty:RGB:clipboard:extkeys:focus:hyperlinks:mouse:osc7:strikethrough:sync:title:usstyle'
set -as terminal-features ',xterm*:RGB:clipboard:extkeys'
set -g set-titles on
set -g set-titles-string "#{?#{m:TMUX*,#{pane_title}},#{pane_title},TMUX - #{pane_title}}"
set -g history-limit 0
set -g window-size latest
set -g aggressive-resize on
set -g exit-empty on
set -g detach-on-destroy on
set -g remain-on-exit off
set -g pane-border-lines single
set -g pane-border-style 'fg=#44475a'
set -g pane-active-border-style 'fg=#44475a'
set -g pane-border-indicators off
set-environment -g COLORTERM truecolor
